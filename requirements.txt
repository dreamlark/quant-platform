# A 股日频量化分析平台 · 依赖清单
# 说明：核心流水线只依赖下方「运行时（必装）」；[ml] / [factors] / [backtest] 为重型可选依赖，
#       模块内全部 try/except 懒加载，未安装时对应能力自动降级，不影响其他模块运行。

# ---- 运行时（必装，已锁定版本线；Python 3.11 / 3.12）----
duckdb>=0.10,<2
pandas>=2.0,<3.1           # ⚠️ 沙箱实测 3.0.0 亦可；代码兼容 3.x
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
statsmodels>=0.14
fastapi>=0.110
uvicorn>=0.29
pydantic>=2
pyyaml>=6
openai>=1.0
tenacity>=8.0
apscheduler>=3.10
loguru>=0.7
python-dotenv>=1.0
httpx>=0.27

# ---- 数据源（纯免费多源；mootdx 主源防封）----
mootdx>=0.10
akshare
baostock

# ---- [ml] 预测模型（D7·核心差异化，懒加载）----
# torch>=2.0
# darts>=0.27
# kronos>=0.1
# huggingface_hub>=0.23
# transformers>=4.40

# ---- [factors] 因子/缠论（懒加载）----
# qlib>=0.9
# czsc>=0.9

# ---- [backtest] 回测/因子体检（懒加载）----
# backtrader>=1.9
# quantstats>=0.0.62
# alphalens-reloaded>=0.4

# ---- dev ----
# pytest>=7.0
# pytest-cov>=4.0
